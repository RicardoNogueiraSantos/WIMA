#include "ChangeDetection.h"

tCompressedData* changeDetect(tChangeDetection* changeDetection, tImage* img) {
	printf("tChangeDetection.changeDetectf is NULL\n");
	
	//tCompressedData* cData = (tCompressedData*) malloc (sizeof(tCompressedData*));
	
	//cData->size = 100*sizeof(unsigned char);
	//cData->data = (uint32_t*) malloc (100*sizeof(uint32_t));
	
	return NULL;
}